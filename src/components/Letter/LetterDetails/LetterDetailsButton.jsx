import React from "react";
import LetterModalButton from "../LetterModal/LetterModalButton/LetterModalButton";

const LetterDetailsButton = ({ onClick }) => {
  return <LetterModalButton onClick={onClick}>CLOSE</LetterModalButton>;
};

export default LetterDetailsButton;
